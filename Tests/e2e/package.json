{
  "name": "opencoregraphics-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser end-to-end tests for the OpenCoreGraphics smoke-test WASM app. Validates CGContext → WebGPU pixel readback in headless Chromium.",
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "serve": "node server.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
